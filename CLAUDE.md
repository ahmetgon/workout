# workout
## Deploy & Secrets
Deploy/altyapı işleri için `deploy.md`'yi izle. **Tüm sırlar Bitwarden'da**, runtime'da `bw get` ile çözülür
(`export BW_SESSION=$(bw unlock --raw)` → ör. `bw get password coolify-admin-token`). Sunucu `65.109.141.55`,
Coolify dashboard SSH tunnel arkasında (`http://localhost:8000`). Repoya / `.env` / `.claude/settings.json`'a
**düz token/parola YAZMA**. Oturum kaydı için `log.md`.
